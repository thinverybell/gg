const express=require('express');const router=express.Router();const {login,issue}=require('../services/authService');router.get('/login',(req,res)=>res.render('auth/login',{error:null,next:req.query.next||'/'}));router.post('/login',(req,res)=>{const u=login(req.body.email,req.body.password);if(!u)return res.status(401).render('auth/login',{error:'Tài khoản hoặc mật khẩu không đúng.',next:req.body.next||'/'});res.cookie('zc_session',issue(u,process.env.SESSION_SECRET||'change-me'),{httpOnly:true,sameSite:'lax',secure:process.env.NODE_ENV==='production',maxAge:7*864e5});const next=req.body.next||'/';res.redirect(next.startsWith('/')?next:'/')});router.post('/logout',(req,res)=>{res.clearCookie('zc_session');res.redirect('/login')});module.exports=router;